import { BaseComponent } from "./base/basecomponent";

export class Shopify extends BaseComponent {

    constructor(element: HTMLElement) {
        super(element);
        this.init();
    }

    init(): void {

    }
}
